# Shared Config Package

This package will hold shared configuration and validation utilities used by API, web, and workers.

Planned contents:

1. Environment variable schema validation
2. Shared constants and policy settings
3. Typed config loaders for each app runtime
